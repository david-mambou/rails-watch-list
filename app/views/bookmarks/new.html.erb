<h2>Add bookmark to list: <%= @list.name %></h2>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>

<%= link_to 'Cancel', list_path(@list) %>
